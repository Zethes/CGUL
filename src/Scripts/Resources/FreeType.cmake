IF(JATTA_USE_FREETYPE)
    ADD_LIBRARY(freetype STATIC IMPORTED)
    SET_TARGET_PROPERTIES(freetype PROPERTIES IMPORTED_LOCATION ${JATTA_DEPENDENCY_DIR}/lib/libfreetype.a)
    SET(JATTA_LIBRARIES ${JATTA_LIBRARIES} freetype)
ENDIF()